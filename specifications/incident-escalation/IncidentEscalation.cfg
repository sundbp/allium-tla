SPECIFICATION Spec

CONSTANTS
    Incidents = {"inc1", "inc2"}
    Teams = {"team1", "team2", "exec"}
    EXEC_NOTIFY_THRESHOLD = 2
    EscalationPolicy = [n \in Nat |-> IF n = 1 THEN "team1" ELSE IF n = 2 THEN "team2" ELSE "exec"]

INVARIANTS
    TypeOK
